
% this file should be included by every subfile, in the \notmain part

% acronyms dont seem to work in subfile mode, make bold to signal
%\newcommand{\ac}[1]{\textbf{#1}}


\input{../acronyms.tex}

% subfile compilation has their own code files in a subfolder code/, compiling from main will create a command {\code}{../x_xxx/code/}
% usage: \lstinputlisting{\codePath name.c}
\newcommand{\codePath}{code/}

% subfile only path
% usage: \includegraphics[scale=1]{\figPath figure.png}
\newcommand{\figPath}{figures/}

\lstset{
	numbers = left,
	breaklines = true,
	tabsize = 3
}
